# Filika Web Application

This directory contains the deterministic demo application and the read-only
maintainer inbox.

Frontend journey, state, wireframe, design-token, accessibility, sample-failure,
and style-isolation foundations are defined in `src/foundation`.
Frontend behavior contracts are defined in `src/contracts`. The application
components consume these contracts as implementation is added.
