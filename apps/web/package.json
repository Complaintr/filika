{
  "name": "@filika/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-sdk": "bun scripts/copy-sdk.ts",
    "build": "bun run --filter @filika/sdk build:development && bun build src/index.html --outdir dist --target browser && bun scripts/copy-sdk.ts",
    "dev": "bun run --filter @filika/sdk build:development && bun dev-server.ts",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@filika/sdk": "workspace:*"
  },
  "devDependencies": {
    "happy-dom": "^20.11.12"
  }
}
