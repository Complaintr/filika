{
  "name": "@filika/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-sdk": "bun scripts/copy-sdk.ts",
    "build": "bun scripts/copy-sdk.ts && bun build src/index.html --outdir dist --target browser && cp public/filika.js dist/filika.js",
    "dev": "bun dev-server.ts",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@filika/sdk": "workspace:*"
  },
  "devDependencies": {
    "happy-dom": "^20.11.12"
  }
}
