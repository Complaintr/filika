{
  "name": "filika",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": "1.3.14"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tests/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "dev:webmcp": "bun --port 4173 apps/web/src/index.html",
    "dev:sdk-smoke": "bun run --filter @filika/sdk build && bun tests/e2e/src/sdk-smoke-server.ts",
    "install:frozen": "bun install --frozen-lockfile",
    "lint": "biome lint .",
    "test:browser": "bun test tests/e2e/placeholder.test.ts",
    "test:unit": "bun test apps packages --pass-with-no-tests",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@types/bun": "1.3.14",
    "ajv": "8.17.1",
    "esbuild": "0.25.12",
    "typescript": "5.9.2"
  }
}
